# Specs — Claude Instructions

The specs folder is the source of truth for this project. See [index.md](index.md) for the full list of spec files.

## Rule: keep code in sync with specs

Whenever any spec file changes, the code must be updated to match before the task is considered done. Never leave a spec change without a corresponding code change, and never change behavior in code without first updating the relevant spec.
